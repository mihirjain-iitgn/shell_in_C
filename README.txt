This assignment is done by:
Name : Mihir Vikram Jain
Roll no : 18110100


Run the make command in the main directory. The binaries of all the commands will be created in ./bin.
The binary of the shell will be made in the main directory itself.

Run ./shell from the main directory. This is important because shell obtains the absolute path on execution to create the relative path for the binaries. After executing, cd command can be used to change directories.

